# 知行工作台 - 帮助手册

> 对应代码版本：v3.1.0

---

## 一、首次启动与密码

### 1.1 首次使用：自行创建密码

知行工作台 **没有默认密码**。首次启动会弹出「欢迎使用知行工作台」引导窗口（SetupPasswordFrame）：

1. 在「请设置密码（至少 6 位）」输入框中输入你要使用的密码
2. 在「请再次输入密码」确认框中重复输入一次
3. 点击「**完成设置**」

> ⚠️ 安全提示（窗口中明确说明）：密码用于加密保护你的待办、笔记、快捷启动数据。请牢记密码，遗忘后数据将无法恢复。

**背后机制**：密码不会写入磁盘明文，只保存 `PBKDF2-HMAC-SHA256` 派生哈希；登录成功后密码在内存中用于解密 AES 加密的 enc_data。

### 1.2 修改密码

1. 左侧导航点击「⚙ 设置」→ 滚动到「🔑 账号安全」区
2. 依次输入：当前密码 / 新密码（≥ 6 位）/ 确认新密码
3. 点击「**确认修改**」

### 1.3 忘记密码

密码不可逆哈希保存，**不可找回**。如确实遗忘：
1. 关闭应用
2. 删除数据目录下的 `config.json`（见第五章「数据目录」）
3. 重启应用 → 回到首次启动引导页重新设置密码（旧加密数据会丢失）

---

## 二、登录与锁屏

### 2.1 正常登录

应用启动（或重新解锁）时弹出「知行工作台 - 登录」窗口：
- 输入密码 → 按回车或点「登录」→ 解密 enc_data 并进入主界面

### 2.2 自动锁屏（v2.3.18+）

防止你走开被人偷看。设置项：
- 入口：⚙ 设置 → 🔒 自动锁屏区
- 选项：**关闭 / 1 分钟 / 3 分钟 / 5 分钟 / 10 分钟**
- 工作方式：全局监听鼠标点击 / 键盘 / 鼠标移动 / 滚轮；每 30 秒 tick 一次；超过阈值 → 自动显示 🔒 锁屏界面
- 若你同时开启了「窗口停靠」：锁屏瞬间主窗口会 `withdraw()` 收纳到系统托盘（更隐蔽）

### 2.3 手动锁屏

三种方式任选：
- **左侧 Logo 区双击**：顶部 Z 字符 Logo 那一行，双击即可
- **系统托盘 → 右键「锁定窗口」**：关闭窗口进托盘后，右键托盘图标 → 锁定窗口
- **主窗口关闭按钮**：默认行为是收纳到托盘（不锁屏），配合自动锁屏使用

### 2.4 解锁

锁屏界面显示 🔒 + 知行工作台 + 「已锁定，请输入密码解锁」：
1. 输入密码 → 回车
2. 密码正确 → 锁屏界面销毁，侧边栏 + 内容区恢复

---

## 三、快捷启动（🚀）

### 3.1 项目类型（三选一）

代码中 `_show_dialog` 顶部有三个 RadioButton：

| 类型 RadioButton | 含义 | 「路径 / 网址 / 命令」栏填什么 |
|---|---|---|
| **软件** | 本机 exe / lnk / 任何可执行文件 | 绝对路径（可用「浏览…」选文件） |
| **网址** | 浏览器打开的 URL | `https://...` 或 `http://...` |
| **系统命令** | Windows 运行命令 | `devmgmt.msc` / `cmd` / `calc` / `regedit` 等 |

选「浏览…」选到 exe 时，**如果名称为空会自动填文件名（去掉 .exe）**。

### 3.2 新增 / 编辑项目

- **新增**：快捷启动页面右上角 → 点击 **「+ 新增项目」**（宽 110px × 34px 蓝色按钮）
- **编辑**：右键任意项目卡片 → 菜单里选「编辑」
- 弹窗标题：新增时为「新增项目」，编辑时为「编辑项目」

### 3.3 分类管理

分类栏在页面顶部（全部 / 默认 / 你的分类…）+ 右侧一个 **⚙** 圆形按钮：

| 操作 | 怎么做 |
|---|---|
| 切换分类 | 点对应分类按钮 |
| 新增分类 | 新增项目时，在分类输入框手动键入新分类名 → 保存后自动加入分类栏 |
| 重命名分类 | **右键分类按钮** → 弹出菜单「重命名」→ 输入新名字（或点 ⚙ → 管理分类弹窗 → 每行有重命名） |
| 删除分类 | **右键分类按钮** → 弹出菜单「删除」→ 二次确认（或点 ⚙ → 管理分类弹窗 → 每行有删除） |
| 批量管理分类 | 点分类栏最右侧 **⚙** → 分类管理弹窗（列表 + 每行操作） |
| 调整分类顺序 | **短按**分类按钮 = 切换分类；**长按或拖移** = 拖拽排序；拖到目标分类上松手即交换顺序（写入 `category_order` 持久化）；「全部」天然固定不可拖 |
| 锁定分类顺序 | 「⚙ 设置 → 导航设置 → 更名设置」关闭开关 → 分类 cursor=arrow、无拖拽、无右键重命名/删除，全面锁定 |

**v3.0.2 新增强**：

1. **跨会话记忆上次分类**（`last_view_category`）：退出时你停在哪个分类，下次登录直接回到这个分类；如果该分类已被删除 → 自动兜底为「全部」。
2. **更名总开关**：默认开启；若担心误操作 → 打开「⚙ 设置 → 导航设置 → 更名设置」关闭开关。关闭后以下操作全部禁用：
   - 快捷方式卡片双击重命名 → 弹提示「更名已关闭」
   - 快捷方式卡片右键菜单「重命名」→ 灰显「重命名（已关闭）」
   - 分类标签右键「重命名/删除」→ 灰显 + 引导开启路径
   - 分类管理弹窗里每行「重命名」「删除」按钮 → 禁用 + 灰显
   - 导航项双击重命名 → 弹提示「更名已关闭」
   - 导航项右键「重命名」→ 灰显「重命名（已关闭）」
3. **新增项目类型选择记忆**（`last_shortcut_type`）：上次选了「网址」，下次打开新增对话框自动选中「网址」。
4. **视图大小记忆**（`last_view_size`）：上次切到「大」视图，下次启动自动恢复「大」。

### 3.4 系统预设

快捷启动页面右上角 → 「系统预设」按钮，一键导入 Windows 常用管理工具：
`设备管理器 · 磁盘管理 · 服务 · 事件查看器 · 系统信息 · 电源选项 · 网络连接 · 程序和功能 · 鼠标属性 · 声音 · 日期和时间`（全部归类到「管理工具」分类）

### 3.5 排序

快捷启动页面右上角 → 「排序」按钮（排序图标），弹出菜单选择排序方式。

### 3.6 图标提取

每个项目卡片的图标：
- **软件**：从 exe 文件提取 Icon 资源，多尺寸选最匹配的裁剪为 64px 圆形图标
- **系统命令**：自动识别对应系统图标（.msc / .cpl / 程序）
- **网址**：尝试抓取 favicon；失败则用首字母 / Z 字符兜底图标
- 统一兜底：Z 字符圆形图标（蓝白配色）

---

## 四、待办事项（📋）+ 番茄钟

### 4.1 任务管理

- **添加**：页面顶部输入框键入任务 → 按回车或点「添加」
- **完成**：点击任务前的复选框 → 文字划线灰掉
- **编辑**：右键任务 → 编辑内容
- **删除**：右键任务 → 删除

### 4.2 番茄钟联动

每任务右侧独立一组番茄钟（一行），结构为：
- **左侧**：`时间标签` — 格式 `「  25:00   │   专注进行中  」`（竖线分隔 + 呼吸感 padding 16px）
- **右侧**：按钮容器（固定 140px 宽）
  - `暂停 / 继续` 按钮（76 × 32，红色系圆角）：开始后文字在「暂停」↔「继续」切换
  - `↺` 重置按钮（32 × 32，灰色圆角）

### 4.3 番茄钟流程（25 分钟 + 5 分钟循环）

```
[启动：点暂停按钮 → 25:00 开始倒计时]
  │
  ├─ 中途暂停：点「暂停」→ 剩余时间冻结；再点「继续」→ 继续走
  ├─ 中途重置：点「↺」 → 回到 25:00 未开始态
  │
  └─ 25 分钟到（专注完成）→ 三件事：
       ① winsound.Beep 三声（叮-叮-咚，880/880/660Hz）
       ② 右下角 Toast 浮窗 360×170：
          标题「🍅 专注完成」· 主文「专注 25 分钟完成」· 副标题「准备进入 5 分钟休息 ☕」
          按钮：立即继续 / 跳过 / 延后 5 分钟
          30 秒不操作 → 自动进入休息
       ③ cycles_completed += 1 · is_break = True · remaining 重置为 5:00
            │
            └─ 5 分钟休息到（休息完成）→ 同样 ①+② 流程：
                 标题「☕ 休息完成」· 主文「休息 5 分钟结束」· 副标题「准备开始下一个番茄钟 🍅」
                 30 秒不操作 → 自动开始下一个 25 分钟专注
```

### 4.4 锁屏 / 最小化不丢计时

PomodoroTimer 是 **wall-clock 绝对时间戳**实现（v2.5.0 引入）：
- 启动瞬间记 `_end_at = time.time() + 25*60`（固定的墙上时钟时刻）
- 每次 tick（900ms）算 `remaining = _end_at - time.time()`，不是每秒 -= 1
- 锁屏 / 最小化期间 `after()` 回调被 Windows 挂起，**但墙上时钟正常走**
- 解锁后第一个 tick：`remaining` 直接跳到真实值（如锁了 10 分钟就从 25:00 变成 15:00）
- 如果番茄钟在锁屏期间到点了，解锁后第一个 tick 检测到 remaining ≤ 0 → 立即触发完成提醒

> 锁屏期间 UI 数字不会实时刷新（回调挂起），但计时本身准确，解锁瞬间会跳到正确时间并弹窗 + 响铃。

---

## 五、笔记（📝）

- **新建笔记**：笔记页面顶部输入标题和内容 → 点「保存笔记」
- **查看笔记**：下方列表按创建时间倒序展示卡片
- **编辑笔记**：右键笔记卡片 → 编辑 → 修改标题/内容后保存（created_at 更新为当前时间）
- **删除笔记**：右键笔记卡片 → 删除 → 二次确认

---

## 六、设置（⚙）

### 6.1 外观

| 设置项 | UI 控件 | 说明 |
|---|---|---|
| 主题 | **SegmentedButton 三选一**（深色 / 浅色 / 跟随系统） | 不是 Switch，是一段式按钮；写入 `config.theme` |
| 窗口停靠 | CTkSwitch（默认开） | 拖到屏幕边缘自动收纳（类似 QQ） |

### 6.2 自动锁屏

| 设置项 | UI 控件 | 说明 |
|---|---|---|
| 时间 | **SegmentedButton 五选一**：关闭 / 1 分钟 / 3 分钟 / 5 分钟 / 10 分钟 | 每 30s tick 检查 idle，到阈值锁屏；关闭 = 0 分钟 |

下方灰色小字提示：
> 无操作多少分钟后自动锁定（防止走开被人偷看）。锁屏后主窗口自动收纳至托盘。

另有一行**状态提示文字**：「未启用自动锁屏」或「已启用自动锁屏：X 分钟」

### 6.3 导航项顺序

- **CTkSwitch**：启用导航拖拽（默认开）
- 关闭后导航项固定，开启后除 🏠 首页 / ⚙ 设置 / ℹ 关于三项**固定不动**，其余可拖拽排序

### 6.4 账号安全

| 输入框 | 说明 |
|---|---|
| 当前密码 | 必填 |
| 新密码 | ≥ 6 位 |
| 确认新密码 | 必须与新密码一致 |

点击「**确认修改**」→ 成功后 toast 右下角提示「密码修改成功」。

---

## 七、窗口停靠（QQ 式靠边收纳）

参数（`install_dock_feature` 内常量，可按需改）：

| 常量 | 值 | 含义 |
|---|---|---|
| EDGE_PX | 14 | 距屏幕左/右边缘多少像素判定为停靠候选 |
| PEEK_PX | 6 | 收起后露出的窄边宽度 |
| FOLD_DELAY_MS | 400 | 鼠标离开窗口多久后收起 |
| UNFOLD_DELAY_MS | 180 | 鼠标碰到收起边多久后展开 |
| UNDOCK_PX | 28 | 展开后拖离边缘 > 28px → 取消停靠 |

流程：
```
拖动窗口到左/右边缘 ≤ 14px
  → 位置稳定 & 鼠标离开窗口 400ms
  → 收起为 6px 窄边（记录收起前位置 normal_geom）
鼠标碰到窄边（或边缘 24px 内）
  → 180ms → 展开到 normal_geom
展开后把窗口拖离边缘 > 28px
  → 取消停靠（回到普通窗口）
```

最小化 / 最大化（zoomed）状态下**不触发停靠**。

---

## 八、系统托盘（pystray）

- **关闭主窗口（点 ×）** → 不是退出，是 `withdraw()` 收纳到系统托盘
- **托盘图标**：Z 字符圆形图标（与任务栏 / 窗口标题栏 / 快捷方式一致）
- **右键托盘菜单**：
  - 显示主窗口
  - 锁定窗口
  - 退出
- **双击托盘图标**：直接恢复主窗口（等同「显示主窗口」）

---

## 九、数据目录

| 部署方式 | 目录路径 | 说明 |
|---|---|---|
| **安装版** | `%USERPROFILE%\.zhixing_workbench\` | 多用户隔离，重装系统前记得备份 |
| **便携版** | `<exe所在同级>\data\` | 放到 U 盘 / 移动硬盘即用，走到哪带到哪 |

自动迁移：便携版启动时检测到安装版目录有数据 → 弹窗询问是否合并 → 是则双向合并（取并集 + 以最新冲突方为准）。

**v3.0.2 便携（绿色）版零留痕强化**：
- 当启动时探测到「EXE 同级 data/ 目录可写」→ 判定为便携模式；
- 此时「⚙ 设置 → 数据管理 → 更改位置 / 恢复默认位置」两个按钮会自动禁用并灰显；
- 下方加橙色提示条：「便携（绿色）版零留痕设计：数据固定在 EXE 同级 data/，不可更改」；
- 目的：防止绿色版在用户不知情时把数据写到主机用户目录，保持真正的零留痕。

### 主要文件

| 文件名 | 内容 |
|---|---|
| `config.json` | 密码哈希、主题、窗口停靠开关、自动锁屏分钟、导航拖拽开关、导航顺序、创建时间 |
| `enc_data` | 登录状态下 AES 加密的 todos / notes / shortcuts |
| `app_settings.json` | UI 偏好（未加密）：`nav_drag_enabled` / `last_view_category` / `category_rename_enabled` / `last_shortcut_type` / `last_shortcut_category` / `last_view_size` / `category_order` / `auto_dock_enabled` / `window_geometry`（窗口大小+位置，v3.1.0 新增） 等 |
| `todos.json` / `notes.json` / `shortcuts.json` | 未登录状态下的临时明文数据（下次登录后会合并进 enc_data 并清空） |

---

## 十、常见问题 FAQ

### Q1：番茄钟锁屏后还会继续吗？
会。基于绝对时间戳（wall-clock），锁屏 / 最小化 / 切页面都不影响。解锁瞬间显示真实剩余时间，并补触发完成提醒（若锁屏期间已到点）。

### Q2：杀毒软件报毒？
PyInstaller 打包 EXE 的常见误报。可从源码自行 `pyinstaller ZhixingWorkbench.spec` 编译。

### Q3：右键「固定到任务栏」后图标是 Python 徽标 / 名字显示 Python GUI？
v3.0 已通过 AppUserModelID v3（进程级 + 窗口级 + 快捷方式三层统一）修复。**请确保使用 v3.0+**；另可尝试：
1. 取消固定 → 重启应用 → 重新固定
2. 清理 Windows 图标缓存（`ie4uinit.exe -show`）

### Q4：忘记密码？
见 1.3 节。密码不可逆，只能删除 `config.json` 重开，旧加密数据会丢失。

### Q5：快捷启动「新增项目」保存后分类没出现在分类栏？
保存后会自动调用 `_rebuild_cat_buttons()` 重建；如果仍看不到，刷新分类栏最右侧 ⚙ → 管理分类弹窗中可核对。

### Q6：添加分类后标签栏按钮「错位重叠 / 显示一个字 / 挤在一起」？
v3.0 已修复（commit c467834）：双保险清空旧按钮 + 中文按钮最小合理宽度兜底。请使用 v3.0+ 版本。

---

## 十一、版本 / 版权

- 版本号：**v3.1.0**（PE 文件头 FileVersion = 3.1.0.0）
- 版权声明（UI 四处 + PE 文件头 LegalCopyright）：
  > **© 2026 知行工作室. All Rights Reserved.**
